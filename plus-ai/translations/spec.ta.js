/*!
 * +AI Specification — Tamil (ta)
 * Status: UNTRANSLATED STUB — awaiting human translation.
 *
 * The interface chrome for this language is already translated inside
 * website-v7.html. Only the 26 specification sections below are outstanding.
 *
 * To contribute: read TRANSLATIONS.md first (it covers the mandatory handling of
 * MUST / MUST NOT / SHOULD / SHOULD NOT / MAY), then populate `sections` and flip
 * `status` to "reviewed". Until then the page shows the English specification and
 * says so explicitly.
 */
(function (registry) {
  registry.register("ta", {
    status: "untranslated",
    sections: [],
    end: null
  });
})(window.PlusAISpecTranslations);